services:
  favicon-fisher:
    image: favicon-fisher:local
    build:
      context: .
      dockerfile: Dockerfile
    container_name: favicon-fisher
    init: true
    ports:
      - "${PORT:-8080}:8080"
    environment:
      PORT: "8080"
    restart: unless-stopped
